---
name: refresh-extensions
description: Refresh the pre-installed extensions table in README
---

## Updating list of pre-installed extensions

In the `README.md`, keep up-to-date the table from section `## Pre-installed extensions`. This table is built from the list of installed extensions in `extensions.sh`.
